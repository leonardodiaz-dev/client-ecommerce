export interface Producto {
    id:string
    title:string
    price:number
    badge:string
    description:string
}

export interface CartItem {
  id: string;
  title: string;
  price: number;
  qty: number;
}